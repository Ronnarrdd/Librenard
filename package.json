{
  "name": "librenard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:links:external": "node scripts/check-links.mjs --external",
    "test": "node --test",
    "verify": "npm run test && npm run build && npm run check:links"
  },
  "devDependencies": {
    "js-beautify": "^2.0.3"
  }
}
